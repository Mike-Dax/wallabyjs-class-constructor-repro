module.exports = () => ({
    autoDetect: true,

    // modify `files` automatic configuration settings
    files: ['src/**/*.ts'],

    // modify `tests` automatic configuration settings
    tests: ['test/**/*.test.ts'],
});